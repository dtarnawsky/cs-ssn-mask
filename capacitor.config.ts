import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.ssn.mask',
  appName: 'cs-ssn-mask',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
